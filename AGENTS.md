# Project guidance

## Demo account privacy

- `wf / password` is private demo access. Do not display it on the sign-in page, front page, or any other user-facing surface.
- The only demo credential that may be shown publicly is `admin / password`.
- Do not remove or change the `wf` account unless the user explicitly asks.
